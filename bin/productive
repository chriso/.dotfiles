#!/bin/sh

# Add a line like:
#
#  0 news.ycombinator.com  # PRODUCTIVE
#
# to your /etc/hosts, and then use `productive` and `unproductive`
# to temporarily block any hosts with the marker

sudo sed -i '' -E 's/^#(0.+PRODUCTIVE)$/\1/' /etc/hosts
